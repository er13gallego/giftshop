import { TablesortDirective } from './tablesort.directive';

describe('TablesortDirective', () => {
  it('should create an instance', () => {
    const directive = new TablesortDirective();
    expect(directive).toBeTruthy();
  });
});
